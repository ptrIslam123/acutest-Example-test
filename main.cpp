#include <iostream>

#include "util.hpp"

int main()
{

    std::cout << "add = " << add(10, 20) << "\n";
    std::cout << "sub = " << sub(10, 20) << "\n";
    std::cout << "mult = " << mult(10, 20) << "\n";

    return 0;
}